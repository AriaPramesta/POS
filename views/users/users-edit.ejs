<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>user add</title>
</head>

<body>
    <form method="post" action="">
        <input type="email" name="email" id="email" placeholder="email" value="<%= item.email %>">

        <input type="text" name="name" id="name" placeholder="name" value="<%= item.name %>">

        <input type="radio" name="role" id="operator" value="operator" <%=item.role=='operator' ? "checked" : "" %> >
        <label for="operator">Operator</label>

        <input type="radio" name="role" id="admin" value="admin" <%=item.role=='operator' ? "" : "checked" %>>
        <label for="admin">Admin</label>

        <button type="submit">Save</button>
    </form>
</body>

</html>